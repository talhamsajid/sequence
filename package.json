{
  "name": "sequence-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @sequence/web dev",
    "build": "pnpm --filter @sequence/web build",
    "dev:mobile": "pnpm --filter @sequence/mobile start",
    "typecheck": "pnpm -r typecheck"
  }
}
